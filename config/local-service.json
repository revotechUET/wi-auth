{
  "Database": {
    "dialect": "mysql",
    // "postgres" for PostgreSql, "mysql" for MySql, "sqlite" for sqLite
    "host": "172.17.0.1",
    "port": 3306,
    "user": "thanh",
    "password": "qwertyuiop",
    "logging": false,
    //Logging all database query if is set "true"
    "storage": "/tmp/wi_backend.sqlite",
    //Path to database.sqlite (used for sqlite only)
    "dbName": "wi_auth"
  },
  "Application": {
    "port": 2999
  }
}

// should ignore from git
